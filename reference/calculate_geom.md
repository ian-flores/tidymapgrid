# Calculate grid based on type and seed

Calculate grid based on type and seed

## Usage

``` r
calculate_geom(file, type, seed)
```

## Arguments

- file:

  The file that has been loaded

- type:

  The type of grid to generate (square or hexagonal)

- seed:

  The seed to generate the grid

## Value

The grid generated
